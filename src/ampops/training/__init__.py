"""Training / experiment-tracking package.

Import submodules directly (`ampops.training.automl`, `.registry`, `.bakeoff`)
so hosts without `h2o` can still import bake-off helpers / registry code.
"""
