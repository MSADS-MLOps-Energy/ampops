# --- Core data & ML ---
pandas
numpy
scikit-learn
xgboost
holidays  # US/IL holiday flag in ampops.features.build
torch

# --- Pipeline orchestration ---
apache-airflow==2.9.3
# prefect  # swap in if using Prefect instead of Airflow

# --- Data versioning ---
dvc
dvc-s3  # or dvc-gs / dvc-azure depending on remote storage

# --- Experiment tracking & registry ---
# Databricks tracking needs the databricks extra (auth via DATABRICKS_* env).
mlflow[databricks]
# H2O AutoML (host-side `make train`). Airflow image pins this in
# requirements-airflow.txt; keep versions in sync when bumping.
h2o==3.46.0.11

# --- Serving ---
fastapi
uvicorn[standard]
pydantic
redis

# --- Weather / external data ---
requests
openmeteo-requests
requests-cache
retry-requests

# --- Monitoring ---
prometheus-client
prometheus-fastapi-instrumentator

# --- Dev tooling ---
ruff
pytest
pytest-cov
python-dotenv

# --- EDA / notebooks ---
matplotlib
seaborn
pyarrow
jupyter
ipykernel
