# --- Core data & ML ---
pandas
numpy
scikit-learn
xgboost
holidays  # US/IL holiday flag in ampops.features.build
torch

# --- Pipeline orchestration ---
apache-airflow

# --- Data versioning ---
dvc
dvc-s3  # or dvc-gs / dvc-azure depending on remote storage

# --- Experiment tracking & registry ---
# Unpinned for local Python 3.13; Airflow image keeps mlflow==2.16.2 in
# requirements-airflow.txt. mlflow 3.x talks to Databricks the same way.
mlflow[databricks]

# --- Serving ---
fastapi
uvicorn[standard]
pydantic
redis

# --- Weather / external data ---
requests
openmeteo-requests
requests-cache
retry-requests

# --- Monitoring ---
prometheus-client
prometheus-fastapi-instrumentator

# --- Dev tooling ---
ruff
pytest
pytest-cov
python-dotenv

# --- EDA / notebooks ---
matplotlib
seaborn
pyarrow
jupyter
ipykernel
